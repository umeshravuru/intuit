# Local Application Configuration
# These are only used when running with -Dspring.profiles.active=local

# Google Cloud Project ID
# REPLACE 'your-project-id' with your actual GCP project ID
gcp.project-id=project-732ea066-e72d-45e9-9ce

# Optional: Path to your Service Account Key JSON file
# If you don't use this, ensure GOOGLE_APPLICATION_CREDENTIALS environment variable is set.
# gcp.credentials.location=/path/to/your/service-account-key.json

# Cache Configuration (Caffeine)
# Expire entries 60 seconds after write
spring.cache.type=caffeine
spring.cache.caffeine.spec=expireAfterWrite=60s

# OAuth2 Resource Server (Clerk)
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://enhanced-oyster-37.clerk.accounts.dev